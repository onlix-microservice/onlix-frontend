version: "3.9"
services:
  vite:
    build: .
    container_name: onlix-frontend
    ports:
      - "3000:3000"
    volumes:
      - .:/app
        #   - /app/node_modules
    working_dir: /app
    tty: true
